/// The amount of players the server can support
pub const MAX_CLIENTS: u32 = 4096;

/// the bandwidth (in bytes) that the client can use
pub const MAX_BANDWIDTH: u32 = 144000;

pub const MAX_MTU: usize = 1400;
